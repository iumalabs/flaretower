// Injected by vite.config.ts's `define` — the repo-root VERSION file's
// contents at build time, or "" when no real release has been baked in
// (local dev; see specs/010-semver-releases/data-model.md).
declare const __APP_VERSION__: string;
